# Runtime Migration Archive

These documents are historical references for the orchestration runtime
migration. They are not the active architecture or migration plan.

Use `docs/orchestration-runtime.md` as the canonical runtime source of truth.

The archived documents are kept because they contain earlier analysis, decision
history, and implementation notes that informed the current plan.
